# Implementation status

Status is derived from current code, generated catalogs, and passing tests.

| Area | Current state | Proof | Release meaning |
|---|---|---|---|
| Context closure | Implemented | 307-record bounded-window snapshot, exact raw cross-binding, eight full-record evidence shards, twelve completed drift reviews, audit/graph/context generators, rich routes, schemas, context tests | Complete local working knowledge; local product is separate from antecedent census |
| Runtime | Implemented with offline attestation and this-host live provider gate | original clean-room runtime, exact synchronous ABI, explicit mode-0600 token file, self-contained bounded GitHub device login/refresh, precise preflight statuses, exact-model catalog, fixed synthetic live tool-loop proof, network-free AttestationProvider, runtime tests | Full exact-public-product live attestation remains future |
| Signed twin-chat | Implemented locally | signed-only child ingress, canonical low-S epoch-bound protocol, crash-phased replay, durable controller outbound journal, live restart tests | Same-user local profile only |
| Actual agents | Implemented | twelve strict agent files, atomic Memory/Factory writes, catalog, live-load and artifact round trip | Preserved as agents in package |
| Controller | Guarded local implementation | one-file phase recovery, no-follow containment, orphan reconciliation, installed-byte adoption, authenticated deterministic `/chat` route/CLI, validated external provider token path passed only in fixed child argv, signed offline SelfTest result proof, and fault tests | No full public-product/live owner attestation |
| Verified adoption | Implemented locally | installed-manifest/loadout/dependency verification, private identity/pairing, tamper and exact-start tests | Requires an explicit local install |
| Key lifecycle | Implemented locally | stopped-only failure-atomic rotation and quarantine/delete/tombstone purge phase tests | File-backed private state |
| Product and instance identity | Implemented and separated | immutable `birth.json`/`rappid.json`, private random instance minting, hatch/adopt tests | Product identity is public; instance identity remains private |
| Dependency lock/fetch | Implemented for target | locks, external-cache fetch tests, artifact injection | No source binaries |
| Packaging chain | Trust resolved locally | pinned P-256 anchor, explicit ephemeral development trust, low-S verify-release, immutable Git source/dependency staging, exact output, SPDX 2.3, same-locked-toolchain repeatability and attack tests | Development trust cannot promote; public attestation pending |
| Fresh-user journey | Implemented locally | doctor/bootstrap, one-command demo, command manifest/docs harness, signed installed-byte attestation, content-free receipt, rollback | No publication, enrollment, or send |
| Isolated egg hatch | Implemented locally | one-descriptor verification/extraction, exhaustive immutable source/venv/site-packages/script/symlink/distribution/RECORD/imsg/loadout inventory, target-free static verification, rollback, and additive `.pth` non-execution tests | Production requires signed release sidecar and expected digest |
| Installed uninstall | Implemented locally | identity confirmation, process/reference checks, dry run, quarantine/delete journal tests | Explicit private roots only |
| iMessage bridge | Implemented, live enrollment pending | pinned payload projection, one-record private account binding/revalidation, pre-ack generation buffering, authenticated global IPC, replay-safe routing, no-GUID unknown echo recovery, bounded terminal supervision, verified-state service uninstall, redacted agent/status, and live in-process tests | No owner enrollment or live-send attestation |
| Static Pages handoff | Implemented locally | exact static inventory, semantic/content checks for every web format, trusted released-state verifier, deep 404, six APIs, safe pending-preservation policy | No live deployment/release |
| Current-tree/output scanners | Implemented | source manifest/privacy scan, bounded nested source archives, generated Pages/artifact/workflow checks, and deterministic iterative Security traversal | Focused runtime scan remains distinct from publication gate |
| Full publication scanner | Implemented; executions unresolved | fixed Git plumbing over canonical branch/tag/configured-remote refs and reachable blobs, commit identities/messages, and tags; exact tracked/untracked/ignored inventory; recursive ZIP/egg/wheel/tar/gzip scan; UTF-8/16 and encoded-form rules; Pages/release/Actions scopes; canonical redacted receipt; pinned detached signature verification | Candidate and final signed receipts require real public inputs and remain `publication-scan` gate evidence |
| Publication | Prepared, unresolved | protected dispatch, main/CI/environment gates, twice-resolved immutable tag, explicit assets/attestations, unchanged prerelease and postflight scripts | No repository settings or public bytes claimed |
| Neighborhood/fleet/cloud | Reference only | matrices | Outside v1 |

The generated implementation matrix covers all 113 capabilities and routes
all 61 selected capabilities with ecosystem status, local claim, direct
evidence heads/drift, major gaps, tested owner or explicit future owner.

The current public snapshot is exactly 307 repositories at existence cutoff
`2026-07-13T08:57:20.399000Z`, observed non-atomically from
`2026-07-13T09:07:31.710577Z` through `2026-07-13T09:09:18.274404Z`.
All twelve required antecedent-head drift reviews are complete at their exact
observed heads. Five repositories that moved after their earlier new-repository
inspection are recorded separately as post-window drift. The local
`rapp-stack-cubby` product was not in the inventory.

Implemented locally does not mean released, externally attested, published, or
conformant. Offline end-to-end local attestation and the content-free live
provider catalog/preflight/tool-loop gate on this host are implemented. The
same proof against an exact public product, live iMessage, public redownload,
and released Pages remain external final gates.
